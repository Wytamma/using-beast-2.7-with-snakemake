# Example using BEAST 2.7 with Snakemake

```bash
snakemake --cores 1 --use-conda -R run_remaster_sir
```

The magic happens in `workflow/envs/beast.post-deploy.sh`.

See the blog for more details -> 
